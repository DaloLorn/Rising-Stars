Subsystem: GateHull
	Name: #GATEHULL
	Description: <<
		Allows certain hulls to override the Gate model. Does not work by default; mods must override getDesignMesh() in scripts/shared/designs.as, replacing the line 
			else if(design.hasTag(ST_Gate) && design.hasTag(ST_Station)) {
		with
			else if(design.hasTag(ST_Gate) && design.hasTag(ST_Station) && !design.hasTag(ST_GateHull)) {
		in order to correctly enable Stargate Integration. (Also, add the DefaultUnlock tag to this subsystem to enable hulls like this, with or without proper integration.)
	>>
	Tags: NoCore, HullSystem, GateHull
	Tags: DefaultUnlock
	EvaluationOrder: -200
	Hull: Flagship,Support,Station

	OnCheckErrors: DOFCommonDesigns::checkGateHull